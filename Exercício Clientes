CREATE FUNCTION somar_clientes_por_dia(data_cadastro DATE)
RETURNS INT
AS
$$
DECLARE
    total_clientes INT;
BEGIN
    SELECT COUNT(*)
    INTO total_clientes
    FROM clientes
    WHERE DATE(data_cadastro) = data_cadastro;
    
    RETURN total_clientes;
END;
$$
LANGUAGE plpgsql;
